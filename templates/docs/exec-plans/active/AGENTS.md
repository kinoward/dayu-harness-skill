# exec-plans/active/AGENTS.md

本目录包含正在执行或待执行的计划。计划应说明目标、约束、步骤、验收标准和当前状态。

## 目录结构

```
active/
├── AGENTS.md       # 你正在读
└── *.md            # 活跃执行计划
```

目录结构变化（含目录、文件或能力部署清单变化）时，必须同步更新本区块。

## 活跃计划

<!-- 按任务列出活跃计划 -->
