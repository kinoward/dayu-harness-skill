# harness/sensors/reviews/AGENTS.md

本目录包含 review 阶段使用的检查清单。Review checklist 是 inferential sensor：AI 或人类按清单检查变更是否满足项目约束。

## 目录结构

```
reviews/
├── AGENTS.md                 # 你正在读
└── code-review-checklist.md  # github.pr
```

目录结构变化（含目录、文件或能力部署清单变化）时，必须同步更新本区块。

## 文档与能力

- `github.pr`：`code-review-checklist.md`
